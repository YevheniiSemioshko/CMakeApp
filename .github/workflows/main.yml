name: Auto Comment on PR

# Запускаем только при открытии нового Pull Request
on:
  pull_request:
    types: [opened]

jobs:
  add-comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write # Разрешение на запись комментариев
    steps:
      - name: Create comment
        uses: peter-evans/create-or-update-comment@v3
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ### 🚀 Новый Pull Request!
            Привет, @${{ github.actor }}! Спасибо за вклад.
            
            **Пожалуйста, проверь следующие пункты:**
            - [ ] Код соответствует стилю проекта.
            - [ ] Добавлены тесты для новой функциональности.
            - [ ] Документация обновлена.
            
            _Этот комментарий добавлен автоматически._
